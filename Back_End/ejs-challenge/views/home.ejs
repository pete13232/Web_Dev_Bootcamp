<%- include('partials/header.ejs') %> 
<h1>Home</h1>
<p><%= startingContent %></p>
<!-- <% for(let i=0; i<posts.length; i++){ %>
    <li><%= posts[i].title %></li>
<% } %>  -->

<% posts.forEach(function(post){ %>
    <h1><%= post.title %> </h1>
    <p><%= post.co"ntent.substring(0,100) + "..." %>
        <a href="/posts/<%= post.title %> ">Read More</a> 
    </p>
<% }) %>
<%- include('partials/footer.ejs') %> 